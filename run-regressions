#! /usr/bin/env bash

if (( $# == 0 )) ; then
	set -- --suite all
fi

./tests/regressions.lua            \
    --testdir   ./tests            \
    --vimrc     ./tests/vimrc.vim  \
    --suitefile ./tests/suites.cfg \
    "$@"
