#! /usr/bin/env bash

if (( $# == 0 )) ; then
	set -- --suite all
fi

./vim-regressions/vim-regressions \
    --testdir   ./tests           \
    --vimrc     regression.vimrc  \
    --suitefile suites.cfg        \
    "$@"
